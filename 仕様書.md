# TacTeQ LINE Harness 運用仕様書

## 概要

TacTeQ 公式 LINE アカウントを [LINE Harness](https://github.com/zegramtool/line-harness-oss)（フォーク）で運用する。

| 項目 | 値 |
|------|-----|
| GitHub | https://github.com/zegramtool/line-harness-oss |
| Worker | https://tacteq-line.tacteq-re-a2f.workers.dev |
| 管理画面 | https://tacteq-line-admin-88e31c57.pages.dev |
| D1 | tacteq-line |
| LINE Bot | @229zfxiy |

## 開発フロー

- 作業場所: `~/Desktop/LINEkoushiki`
- `main` はブランチ保護（PR 必須）→ マージで Cloudflare 自動デプロイ
- 本家取り込み: GitHub Actions → Update from upstream

### Cursor Cloud Agent から Worker デプロイする設定

**運用方針（確定）:** Cloudflare の秘密情報は **GitHub Actions の Secrets にだけ置く**。  
Cloud Agent 環境への `CLOUDFLARE_API_TOKEN` 登録は不要。デプロイ・R2・D1 更新は Actions 経由で行う。

| やりたいこと | 手段 |
|---|---|
| Worker デプロイ | `main` マージ → `Deploy Cloudflare Worker` |
| PDF アップロード / フォーム fields 更新 | `TacTeQ Ops`（関連 path の `main` push で自動、または Actions 画面から手動） |

（参考）もし Cloud Agent 直実行に切り替える場合だけ、[Cloud Agents ダッシュボード](https://cursor.com/dashboard/cloud-agents) の Secrets に `CLOUDFLARE_API_TOKEN` 等を追加する。現行運用では使わない。

## 応答設定（LINE Official Account Manager）

| 項目 | 設定 |
|------|------|
| チャット | ON |
| Webhook | ON |
| あいさつメッセージ | **OFF**（Harness シナリオで送信） |
| 応答メッセージ | OFF |

## 友だち追加時の自動メッセージ（4通・即時）

1. 挨拶テキスト（`{Nickname}` / `{AccountName}` = 表示名 / TacTeQ）
2. お見積り用写真撮影ガイド（画像）
3. オレンジ Flex ボタン「お問合せフォーム入力」→ Harness LIFF フォーム（3ステップ）
4. 注意書き（URL なし）

- シナリオ名: **友だち追加ウェルカム**（`friend_add`）
- セットアップ: `pnpm tacteq:activate-inquiry-form` → `pnpm tacteq:activate-welcome`
- 画像差し替え: `scripts/tacteq/assets/estimate-photo-guide.png` を更新 → `pnpm tacteq:upload-welcome-image`
- フォーム設定: `scripts/tacteq/tacteq-form.json`（Form ID / LIFF URL）

## お問い合わせフォーム（Harness LIFF）

| 項目 | 値 |
|------|-----|
| フォーム名 | TacTeQ お問い合わせ |
| Form ID | `fd2d74cb-ef42-46fd-9960-d0725674d46a` |
| LIFF URL | `https://liff.line.me/2010377322-vRCNhYfx?page=tacteq-inquiry&id=fd2d74cb-ef42-46fd-9960-d0725674d46a` |
| 送信後タグ | `問合せ済`（フォーム送信時に自動付与） |

### 営業ステータスタグ（手動付与）

| タグ | 用途 |
|------|------|
| 見積もり済み | 見積提示後 |
| 依頼 | 正式依頼・受注 |
| 完了 | 作業完了 |
| 入金済み | 入金確認 |
| 失注 | 失注・見送り |

- **タグの追加・削除:** 管理画面 → **タグ管理**（`/tags`）
- **友だちへの付け外し:** **友だち管理** または **個別チャット**（⋮ メニュー）
- 一括作成: `pnpm tacteq:setup-pipeline-tags`
| 回答の確認 | 管理画面 → **分析 → フォーム回答** →「TacTeQ お問い合わせ」 |
| データ保存先 | Cloudflare D1 `form_submissions` テーブル（友だち `metadata` にもマージ） |
| 項目ラベル表示 | 回答詳細・一覧は `forms.fields` の日本語ラベルを優先。無いキーは `apps/web/src/lib/form-field-labels.ts` のフォールバック（例: `target_area`→対象箇所、`request_detail`→ご要望）。個別チャットの友だち情報 metadata も同様 |

### 入力ステップ（全6画面）

1. **今回のご相談**（傷の修繕 / 劣化 / リフォーム / 床鳴り / 塗装 / その他）
2. **対象箇所**（**複数選択可**。相談種別で出し分け）
   - 「自由入力」「その他」を選んだ場合、詳細テキストが **対象箇所** 欄にそのまま反映される（「自由入力」という固定文字は保存しない）
3. **お客様・物件**（業者/個人、住居種別、**原因は？**、工事中かどうか）
   - 住居種別: 持家（戸建て） / 持家（マンション） / 賃貸・借家 / 店舗
   - 原因は？: 業者様がお客様先で傷をつけた / お客様ご自身で傷をつけた / 経年劣化 / その他
4. **基本情報**（お名前、フリガナ、郵便番号→住所自動入力、**物件の所在地**）
5. **ご希望・スケジュール**（一番の目的、完了期日、作業希望日、ご要望、**ご予算感（任意・自由入力）**、初めてか等）
6. **連絡・確認**（連絡手段は複数選択可、火災保険、個人情報同意チェック）
   - 連絡手段: LINE / 電話 / ショートメール / メール（「どれでも」は廃止）
   - 電話番号欄は常時表示。「お急ぎの方は電話番号を入力してください」の案内あり
   - 電話・ショートメール選択時 → 電話番号必須 / メール選択時 → メールアドレス必須

送信後、**トークルームに自動送信**される（管理画面の個別チャットにも表示）:

1. **お問い合わせ内容**（Flexカード — 入力内容が一覧で表示）
2. 写真・**動画**送付のお願い（テキスト — 動画歓迎の文言あり）
3. お見積り用写真の撮影見本（ウェルカムと同じ画像 `welcome-estimate-photo-guide.png`）
4. **（条件付き）作業の流れ案内＋傷リペアの作業の流れ PDF** — 相談が「傷の修繕」または「劣化の修繕」のときのみ。テキスト「作業の流れはこちらです。」の直後に「PDFを開く」Flex ボタン

### 相談種別ごとの PDF 自動送信

| 項目 | 値 |
|------|-----|
| 対象相談 | 傷の修繕 / 劣化の修繕 |
| 送信位置 | 写真撮影のお願い（テキスト＋見本画像）の**直後** |
| 案内テキスト | `作業の流れはこちらです。`（`REPAIR_FLOW_INTRO_TEXT`） |
| ファイル | `scripts/tacteq/assets/repair-flow.pdf` |
| R2 キー | `tacteq-repair-flow.pdf`（期限なし。チャットPDFの `files/` 配下ではない） |
| 公開URL | `https://tacteq-line.tacteq-re-a2f.workers.dev/images/tacteq-repair-flow.pdf` |
| アップロード | `pnpm tacteq:upload-repair-flow-pdf` |
| 実装 | `apps/worker/src/services/tacteq-form-reply.ts` |

差し替え手順: assets の PDF を更新 → `pnpm tacteq:upload-repair-flow-pdf` → Worker デプロイ（返信ロジック変更時のみ）。
### 送信通知（管理者 LINE）

フォーム送信時、登録済みの管理者 LINE ユーザーへプッシュ通知。

```bash
# 通知先 = TacTeQ 公式アカウントの友だちである LINE ユーザー ID（U で始まる）
TACTEQ_NOTIFY_LINE_USER_IDS=Uxxxxxxxx,Uyyyyyyyy pnpm tacteq:activate-form-notification
```

※ 管理者は事前に TacTeQ 公式 LINE を友だち追加しておくこと。

### Notion バックアップ

フォーム送信のたびに Notion データベースへ1行追加される。

| 項目 | 値 |
|------|-----|
| DB 名 | **LINE Harness 相談データベース** |
| Notion URL | https://app.notion.com/p/14b79bcdc6ae457fa8807128439d42ac |
| Database ID | `14b79bcd-c6ae-457f-a880-7128439d42ac`（D1 登録済み） |

旧 Notion フォーム DB（`TacTeQへお問合せ`）とは別。Harness LIFF フォーム専用。

**残り1ステップ（Worker から自動書き込み用）:**

1. [Notion Integrations](https://www.notion.so/my-integrations) で Internal Integration を作成（例: `TacTeQ LINE Harness`）
2. 「LINE Harness 相談データベース」をインテグレーションに **コネクト**
3. トークンを Worker に登録:

```bash
cd apps/worker && npx wrangler secret put NOTION_API_TOKEN
```

4. 接続テスト:

```bash
NOTION_API_TOKEN=secret_xxx NOTION_DATABASE_ID=14b79bcd-c6ae-457f-a880-7128439d42ac pnpm tacteq:activate-notion-backup --test
```

Notion 未設定時もフォーム送信自体は正常に完了する（バックアップのみスキップ）。

過去分の Notion 反映:

```bash
NOTION_API_TOKEN=secret_xxx pnpm tacteq:backfill-notion
```

**技術メモ:** 管理者通知・Notion バックアップは `executionCtx.waitUntil` で非同期完了させる（未設定だと Worker がレスポンス後に処理を打ち切る）。

### セキュリティ・整合性（2026-06）

| 項目 | 対応 |
|------|------|
| チャット status/メモ | `consolidateChatsForFriend` で重複 `chats` 行を統合。`in_progress` > `unread` > `resolved` で status をマージ |
| フォーム送信 | TacTeQ 問い合わせは **常に LIFF `id_token` 必須**（匿名 POST 不可）。副作用ありフォームも同様 |
| モバイル認証 | APIキーは保存せずセッショントークン（`lh_sess_*`、**最大7日**・ログアウトで失効）。既定は **この端末に記憶**（`localStorage`）。オフにするとタブ閉じで再ログイン |
| チャット CSV | `line_oa_chat_csv_*` は **`.gitignore` 対象**（PII をリポジトリに含めない） |
| CSV 日時 | インポート時 JST パースを `jstNow()` と同じ方式に統一（9時間ずれ修正） |

**注意:** 過去に Git に CSV をコミットした場合は `git filter-repo` 等で履歴から除去すること。ローカルフォルダはリポジトリ外で保管。

## 管理画面（iPhone 対応）

- URL: https://tacteq-line-admin-88e31c57.pages.dev/login
- **ログイン後の初期画面は個別チャット**（`/chats`）。トップ `/` も `/chats` へリダイレクト
- ダッシュボードはメニューから `/dashboard` で開ける
- iPhone / Android では **Bearer 認証（セッショントークン `lh_sess_*`）** を使用（Cookie は使わない）
- ログイン画面の **「この端末に記憶する」**（既定オン）ならタブを閉じても最大7日入れる。オフなら従来どおりタブ閉じで再ログイン
- 左上 **≡ メニュー** → **ヘルプ** / 個別チャット / フォーム回答

### ホーム画面アイコン（2026-08）

iPhone はホーム画面に追加するとき、アイコンに **角丸マスクを自分でかけます**。画像側でも角を丸めると、二重に丸くなって形がおかしく見えます。

そのためアイコン画像は **四角い緑の全面塗り（角丸なし）** の PNG にしています。白い吹き出しは中央に置いてあります。

| ファイル | サイズ | 用途 |
|----------|--------|------|
| `/apple-touch-icon.png` | 180×180 | iOS が自動で探すホーム画面アイコン |
| `/icons/apple-touch-icon.png` | 180×180 | `<link rel="apple-touch-icon">` |
| `/icons/icon-192.png` / `icon-512.png` | 192 / 512 | PWA manifest |

**すでにホーム画面に追加済みの場合、iOS は古いアイコンをキャッシュします。** 形を直すには:

1. ホーム画面のアイコンを長押しして削除
2. Safari で管理画面を開き直す
3. 共有 → **ホーム画面に追加**

### 未読バッジ（2026-08）

チャット一覧の赤点と同じく、**status が `unread` の友だち件数**です。「未対応」メニューの件数とは別です。お客さんの自発メッセージは、**対応中でも未読に戻す**（開いたとき誰から来たか分かるようにする）。

| 場所 | 動き |
|------|------|
| ホーム画面アイコン | iOS 16.4+ のホーム画面 Web アプリで赤い数字。Badging API（`setAppBadge`） |
| 左上 ≡ | 未読があるとき件数バッジ |
| メニュー「個別チャット」 | 同じ件数 |

- API: `GET /api/chats/unread-count`（ログイン必須）
- 管理画面を開いているあいだ、約60秒ごと＋画面を前面に戻したときに更新
- トークで「未読 / 対応中 / 解決済」を変えた直後も更新
- **アプリを完全に閉じたまま** 未読を届ける: **Web Push**（下の節）

### Web Push（2026-08）

iPhone は、ホーム画面に追加した Web アプリ（iOS 16.4+）で **通知を許可** すると、アプリを終了していても未読を受け取れる。

仕組み（大学の授業でいうと「郵便受け」）:

1. 管理画面が Service Worker（`/sw.js`）を登録する
2. 「通知をオン」でブラウザが Push 購読を作る
3. Worker が VAPID 鍵を D1 に保存し、お客さんの自発メッセージ（auto_reply に当たらないテキスト）で `unread` になったとき、購読先へ送る
4. iOS 18.4+ は Push 本文の `web_push: 8030` と `app_badge` を OS が直接読んで通知と数字を付ける。古い iOS / Chrome は Service Worker が同じ本文から通知と `setAppBadge` をする

| 項目 | 内容 |
|------|------|
| オンにする場所 | ≡ メニュー下部の **「通知をオン（未読バッジ）」** |
| 条件 | ホーム画面から開いていること（Safari のタブだけだと iOS は許可しない） |
| 通知 | タイトルは友だちの名前。タップするとその人のトーク（`/chats/?friend=`）を開く |
| バッジ | **iOS 18.4+** は Push JSON の `app_badge`（Declarative Web Push。アプリを完全終了していても OS が数字を付ける）。それ以前は SW の `navigator.setAppBadge`（スワイプ終了すると付かないことがある） |
| 鍵 | Cloudflare の追加シークレットは不要。初回 API で VAPID 鍵を D1 `web_push_vapid` に作る |
| 購読 | D1 `web_push_subscriptions` |
| API | `GET /api/web-push/vapid-public-key` / `POST`・`DELETE /api/web-push/subscribe`（ログイン必須） |

VAPID 公開鍵は `Uint8Array` で `pushManager.subscribe` に渡す（`ArrayBuffer.slice` だと Next.js の型チェックで落ちる）。

**使い方:** 本番反映後、ホーム画面のアイコンから開く → ≡ → 「通知をオン」→ 許可。テストは、別の端末から公式 LINE にテキストを送ると iPhone に通知が来る。

Service Worker を更新したあとは、**一度ホーム画面アイコンからアプリを開いて閉じる**（新しい `sw.js` を取り込む）。通知は来るのに数字が付かないときは:

1. iPhone の **設定 → 通知 → チャット → バッジ** をオン（**即時通知** はバナー用で、バッジとは別スイッチ）
2. ホーム画面アイコンからアプリを開き直す（Push 購読は `/sw.js` に固定。`?v=` 付きは別 Worker になり、通知だけ古い方に届く）

お客さんの自発メッセージ（テキスト・画像・スタンプ等）は対応中でも `unread` に戻す。通知のタイトルは名前、タップ先は `/chats/?friend=<id>`。一覧では赤い点と太字プレビューで誰か分かる。ステータス変更では Push を送らない。

iOS はアプリをスワイプで完全終了すると、Service Worker の `setAppBadge` が動かない（通知バナーだけ来る）。そのため本番の Push 本文は **Declarative Web Push**（`web_push: 8030`, `mutable: false`, `notification.app_badge`）にする。数字が付くのは **iOS 18.4 以降**。設定の **バッジ** もオンが必要。

### 個別チャットからの送信

| 種類 | PC / iPhone | 備考 |
|------|-------------|------|
| テキスト | ○ | |
| 画像 (JPEG/PNG) | ○ | 最大10MB・**1回最大5枚**まとめて送信。1MB超はプレビュー自動圧縮。HEIC非対応。トーク内タップで**拡大プレビュー・保存** |
| PDF | ○ | 最大 20MB。**リンク有効期限 30日**（`CHAT_PDF_TTL_DAYS` で変更可）。期限後は R2 から自動削除し URL は 410。LINE には **「PDFを開く」Flex ボタン**（期限の注記付き） |

### トーク内の URL / 地図（2026-09）

お客さんが送った **https のリンク**（Google マップの短縮 URL 含む）は、トークの文字のままでは iPhone から開けなかった。本文中の `http://` / `https://` をタップできるリンクにする。

LINE の **位置情報ピン** は、以前は `[位置情報: 名前]` の文字だけだった。緯度経度を保存して **地図を開く**（Google マップ）を出す。すでに文字だけ残っている過去分は座標がないので開けない。

実装: `apps/web/src/lib/chat-text-links.ts` / `packages/shared/src/location.ts`

### お客さんが送った PDF / ファイル（2026-08）

以前はトークに `[ファイル: 名前.pdf]` の文字だけ残り、開けなかった。画像と同じく **LINE Content API で本体を取得して R2 に保存**する。

| 項目 | 内容 |
|------|------|
| 対象 | LINE の file メッセージ（PDF が主。docx 等も保存して「開く」） |
| 保存 | PDF は送信 PDF と同じ `files/{uuid}.pdf`。期限は付けない（受信画像と同じく残す） |
| 画面 | ファイル名＋サイズ、埋め込みプレビュー、**プレビュー**（拡大）、**保存** |
| 上限 | 20MB。超えると今までどおりファイル名の文字だけ |
| 過去分 | すでに文字だけ残っているものは、LINE 側の本体保持期限と messageId 未保存のため **復元できない**。これ以降に届いた分から開ける |

実装: `apps/worker/src/services/incoming-file.ts` / `apps/web/src/components/chats/chat-pdf-bubble.tsx`

### 今すぐ送信の取り消し（2026-08）

LINE には「送ったあとに取り消す」API がない。代わりに **「今すぐ」は 12秒待ってから送信** し、その間だけ取り消せる。

| 項目 | 内容 |
|------|------|
| 対象 | 「今すぐ」のテキスト / 画像 / PDF（予約送信は従来どおり日時指定） |
| 待ち時間 | **12秒**（`UNDO_SEND_DELAY_MS`） |
| 画面 | 緑のバナー「あと N 秒」＋ **取り消し**（確認ダイアログなし。複数件なら「すべて取り消し」） |
| 取消時 | お客さんの LINE には届かない |
| 12秒後 | Worker の `waitUntil` と画面側の配信 API が送信。先に claim した方が勝ち（二重送信しない） |
| タブを閉じた場合 | Worker 側の遅延送信が続ける。万一止まっても 5分 Cron が拾う |
| 保存 | 通常予約と同じ `scheduled_messages`（作成〜送信が 20秒以内なら取り消し付きと判定） |

実装: `packages/db/src/scheduled-messages.ts` / `apps/worker/src/routes/chats.ts` / `apps/web/src/app/chats/page.tsx`

### 個別チャット予約送信（2026-06）

管理画面 → **チャット** → 個別トークで **「予約」** を選び日時を指定すると、その友だちへ指定時刻以降に自動送信される（5分 Cron、最大5分遅れ）。

| 項目 | 内容 |
|------|-----|
| 対象 | テキスト / 画像 / PDF |
| 複合予約 | **PDF＋テキスト** / **画像＋テキスト** も即時送信と同じく、種別ごとに別レコードとして予約される（1回の操作で複数件作成） |
| 編集 | 予約中一覧の **編集** → 入力欄に内容・日時を読み込み → **更新**（pending のみ）。`PATCH /api/scheduled-messages/:id`。編集中に別種別を追加した場合は、既存1件を更新し追加分は新規予約 |
| 取消 | 予約中一覧の **予約取消** ボタン（確認ダイアログあり、pending のみ） |
| 保存 | D1 `scheduled_messages`（1行 = text / image / file / flex のいずれか1つ） |
| マイグレーション | `packages/db/migrations/047_scheduled_messages.sql` |

#### 既知不具合の修正（2026-07）

- **症状**: PDF（または画像）を添付してテキストも入力し「予約」すると、PDF/画像だけが予約され、テキストは API に送られず入力欄クリアで消える
- **原因**: 予約送信フロントが `if (PDF) else if (画像) else if (テキスト)` の排他分岐だった（即時送信は独立に複数送信）
- **修正**: `apps/web/src/app/chats/page.tsx` の予約パスを即時送信と同様、種別ごとに `api.chats.send(..., { scheduledAt })` を複数回呼ぶように変更。組み立ては `buildScheduledPayloads`（`apps/web/src/lib/build-scheduled-payloads.ts`）に集約し、単体テストで PDF+テキストの両方が出ることを担保

**ストレージ制限（現状）**

| 項目 | 値 |
|------|-----|
| 1ファイル上限 | 20MB |
| リンク有効期限 | **30日**（アップロードから。Worker 環境変数 `CHAT_PDF_TTL_DAYS`） |
| 期限後 | URL は開けない（410）。6時間ごとの cron で R2 から削除 |
| アカウント全体の容量上限 | **コード上は未設定**（Cloudflare R2 のプラン枠に依存） |

### 管理画面のよくある表示

| 表示 | 原因 | 実害 |
|------|------|------|
| 「改造を検知しました」 | upstream Harness とビルドハッシュが一致しない（**フォーク運用では正常**） | なし。自動アップデートが無効になるだけ。`next.config.ts` でバナーは既定 **OFF** |
| アップデート履歴が空 | 自動アップデート未使用（手動デプロイ運用） | なし。履歴 API はスタッフ認証の `/api/updates/history` を使用 |

管理画面を更新したら本番反映:

```bash
NEXT_PUBLIC_API_URL=https://tacteq-line.tacteq-re-a2f.workers.dev pnpm --filter web build
npx wrangler pages deploy apps/web/out --project-name=tacteq-line-admin-88e31c57 --branch=main
```

### 未対応インボックス

メニューの「未対応」と、トーク一覧の **返信待ち** は同じ意味です。**Harness から手動返信していない incoming** を数えます。未読・対応中・解決済とは別です。CSV インポート履歴（`csv_import`）は **対象外**（2026-06 修正）。

トーク一覧は **最新メッセージ順**。`chats` 行が無くても、最後がお客さんの incoming（テキスト・画像・スタンプ等）なら未読として出す。画像だけの受信でも未読と Web Push を付ける。

一括整理:

```bash
pnpm tacteq:clear-unanswered --dry-run
pnpm tacteq:clear-unanswered
```

## TacTeQ 用コマンド一覧

| コマンド | 用途 |
|---------|------|
| `pnpm tacteq:activate-inquiry-form` | 問い合わせフォーム作成・更新 |
| `pnpm tacteq:activate-form-notification` | フォーム送信時の管理者 LINE 通知先を登録 |
| `pnpm tacteq:activate-notion-backup` | Notion DB へのバックアップ先を登録 |
| `pnpm tacteq:activate-welcome` | ウェルカム4通（ボタンURLは tacteq-form.json を参照） |
| `pnpm tacteq:upload-welcome-image` | お見積り写真ガイド画像を R2 へアップロード |
| `pnpm tacteq:upload-repair-flow-pdf` | 傷リペア作業フロー PDF を R2 へアップロード |
| `pnpm tacteq:update-inquiry-form-fields-d1` | フォーム fields を D1 直更新（APIキー不要） |
| `pnpm tacteq:sync-followers` | 既存フォロワーを D1 に同期 |
| `pnpm tacteq:setup-pipeline-tags` | 営業パイプライン用タグ（見積もり済み等）を一括作成 |
| `pnpm tacteq:clear-unanswered` | 未対応インボックスを一括クリア（対応済み整理） |

## データ移行オペレーション

### 既存友だちの同期

```bash
pnpm tacteq:sync-followers          # D1 の line_accounts からトークン取得
pnpm tacteq:sync-followers --dry-run
```

LINE API `followers/ids` でフォロー中ユーザーを `friends` テーブルに upsert。

### チャット履歴インポート（LINE 公式アカウント管理画面 CSV）

LINE 管理画面からダウンロードした **1友だち1CSV** 形式（先頭3行メタ＋`送信者タイプ,送信者名,送信日,送信時刻,内容`）に対応。

1. チャット履歴をエクスポートしてフォルダに展開（例: `line_oa_chat_csv_260612_193545/`）
2. 友だち同期を先に実行（`pnpm tacteq:sync-followers`）
3. インポート:

```bash
pnpm tacteq:import-chat --dir /path/to/csv-folder
pnpm tacteq:import-chat --dir /path/to/csv-folder --replace   # 既存 csv_import 行を削除して再投入
pnpm tacteq:import-chat --file sample.csv --inspect   # 列確認
pnpm tacteq:import-chat --dir ... --dry-run
```

**2026-06-12 実施:** `line_oa_chat_csv_260612_193545` から 181 ファイル・約 5,011 件を `messages_log` に投入。

**2026-06-12 再インポート（JST 9時間ずれ修正）:** `--replace` で csv_import 由来データを削除後、同フォルダから再投入済み。

**制限:** テキストのみ復元（「写真を送信しました。」等のプレースホルダー）。画像・スタンプ・Flex は元ファイルなしのため不可。Harness 導入後の新規メッセージと重複する場合あり。

## 2026-06-12 実施済み

- フォーク clone・GitHub Actions デプロイ設定
- `main` ブランチ保護
- ウェルカムシナリオ有効化 + ステップ追加
- 既存フォロワー 224 名を D1 に同期
- iPhone 向け管理画面ログイン（Bearer フォールバック）
- TacTeQ 3ステップ問い合わせ LIFF フォーム + ウェルカムボタン差し替え
